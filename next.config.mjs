/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', 'cdn.iconscout.com', 'media.licdn.com']
    }
};

export default nextConfig;
